#include<stdio.h>
int factorial(float);
int main()
{  int n;
  printf("Enter a no. \n");
  scanf("%d",&n);
  printf("Factorial of %d is %d"n,(fact(n));
}
int factorial(float n)
{ if(n>0)
  return(m=n*fact(n-1));
  else
  return(1);
}
