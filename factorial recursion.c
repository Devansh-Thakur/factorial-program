#include<stdio.h>
int fact(float)
int main()
{
  printf("Enter a no. \n");
  scanf("%d",&n);
  printf("Factorial of %d is %d."n,(fact(n));
}
int factorial(int n)
{ if(n>0)
  return(m=n*fact(n-1))
  else
  return(1);
}
